<h2><%= @year %>年<%= @month %>月<%= @day %>日のイベント</h2>
<% @events.each do |e|%>
<% if ( (e.year.to_i == @year.to_i) && (e.month.to_i == @month.to_i) && (e.date.to_i == @day.to_i) ) then %>
<h4>「<%= link_to "#{e.title}", "/events/show/#{e.id}" %>」</h4>
<p><%= simple_format(e.about) %><p>
<% if e.user_id == current_user.id then %>
<p>
<%= link_to "[削除]", "/events/delete/#{e.id}", data: { confirm: "「#{e.title}」を削除しますか?"} %>
<%= link_to "[編集]", "/events/edit/#{e.id}" %>
</p>
<% else %>
<p>[削除・編集できません]</p>
<% end %>
<% else%>
<% end %>
<% end %>
