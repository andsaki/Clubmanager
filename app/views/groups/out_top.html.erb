<section class = "column">
<% @group = Group.where("id = ?", @group_id).first %>
<h1>「<%= @group.name %>」</h1>
<p><%= simple_format(@group.about) %></p>


<% @member = Member.where("group_id = ?", @group_id).where("user_id = ?", current_user.id).first %>

<p>
<% if @member == nil then %>

<%= link_to "申請する", "/members/applicate/#{@group_id}" %>

<% elsif @member.p == 0 %>

<% @group = Group.where("id = ?", @group_id).first %>
<p>現在「<%= @group.name %>」への参加を申請中です。</p>
<p>管理者の承認をお待ちください。</p>
<p><%= link_to "申請を取り消す", "/groups/delete_member/#{@group_id}"%>

<% else %>
<%= link_to "入場する", "/groups/top/#{@group.id}" %>
<% end %>
</p>

</section>
