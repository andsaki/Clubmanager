<h2>「<%= current_user.username%>」さんの管理団体</h2>
<% @group = Group.where("master_id = ?", current_user.id) %>
<ul>
<% @group.each do |f| %>
<li>
<%= link_to "#{f.name}", "/members/applicater/#{f.id}" %>
</li>
<% end %>
</ul>
